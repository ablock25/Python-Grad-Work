#Define, directly in the interactive shell, function perimeter()
#that takes, as input, the ra- dius of a circle (a nonnegative number)
#and returns the perimeter of the circle.

import math

def perimeter(x):
    res = math.pi * x**2
    return res
